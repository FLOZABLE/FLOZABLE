import React from "react";
import styles from "./ChatGroupSelector.module.css";

function ChatGroupSelector(groups) {
  return (
    <div className={styles.ChatGroupSelector}>

    </div>
  );
};

export default ChatGroupSelector;